# 餐猎平台 — 交付文档

> 餐饮行业猎头招聘平台 · 交付时间：2026-08-12

---

## 一、项目概述

**餐猎**是面向餐饮酒店行业的高端人才招聘平台，连接求职者（人才）与企业，并提供供应链、内容分享等增值服务。

| 项 | 说明 |
|---|---|
| 技术栈 | React 18 + Vite + TypeScript + TailwindCSS（前端）／ Node.js + Express + Prisma + SQLite + Socket.IO（后端） |
| 部署 | PM2 进程管理 + Cloudflare Tunnel 公网访问 |
| 用户角色 | `TALENT` 人才 · `ENTERPRISE` 企业 · `ADMIN` 管理员 |

---

## 二、访问地址

| 环境 | 地址 |
|---|---|
| 公网访问 | https://feeling-fancy-results-selecting.trycloudflare.com |
| 本地前端 | http://localhost:5173 |
| 本地后端 | http://localhost:3001 |
| 健康检查 | https://feeling-fancy-results-selecting.trycloudflare.com/api/health |

> ⚠️ **Cloudflare Tunnel 免费版**：每次隧道重启 URL 会变。查看最新地址：
> ```bash
> tail -20 logs/tunnel-out.log | grep trycloudflare
> ```

---

## 三、测试账号

| 角色 | 手机号 | 密码 | 说明 |
|---|---|---|---|
| 管理员 | `13800000000` | `通过安全渠道分发` | 后台审核、星级评定 |
| 企业 1 | `13800000001` | `通过安全渠道分发` | 湘味轩餐饮集团（已认证） |
| 企业 2 | `13800000002` | `通过安全渠道分发` | 悦榕庄酒店（已认证） |
| 企业 3 | `13800000003` | `通过安全渠道分发` | 星瀚餐饮（筹备阶段） |
| 人才 1 | `13900000001` | `通过安全渠道分发` | 王建国 · 行政总厨 |
| 人才 2 | `13900000002` | `通过安全渠道分发` | 刘明 · 厨师长 |
| 人才 3 | `13900000003` | `通过安全渠道分发` | 陈伟 · 餐饮总监 |
| 人才 4 | `13900000004` | `通过安全渠道分发` | 赵强 · 中餐主厨 |

---

## 四、功能模块

### 核心功能
- **认证系统**：手机号 + 密码登录、JWT 鉴权、忘记密码（验证码重置）
- **人才端**：简历编辑、职位搜索筛选、投递、收藏、AI 智能匹配、实时聊天
- **企业端**：企业信息与认证、发布职位、人才搜索、查看投递/匹配、实时聊天
- **管理员**：企业认证审核、人才/企业星级评定、供应审核、分享审核
- **实时聊天**：Socket.IO 双向沟通、消息已读、未读计数

### 本期新增模块
1. **供应平台** — 10 个分类（食材/餐具/厨具/家具/品牌策划/设计/培训/出租转让/二手/投资），商家入驻（营业执照审核）、发布带图带价产品
2. **创业分享 / 学习分享** — 视频号风格信息流，支持图片+文字+视频发帖、点赞、评论
3. **AI 匹配增强** — 新增性别/任职时长/企业画像等维度，15 维度综合评分（权重和=1.00），修复学历匹配枚举 bug

---

## 五、安全加固（本期完成）

经四维安全审计（网络安全 / 隐私 / 授权 / 维护），9 项发现已全部修复：

### 网络安全
- ✅ 修复验证码明文返回和固定万能码漏洞（原可接管任意账号）→ 改为服务端一次性随机码摘要存储，不回传客户端
- ✅ JWT_SECRET 生产环境强制配置（无密钥拒绝启动）
- ✅ 加 `helmet` 安全响应头（CSP / HSTS / X-Frame-Options / nosniff）
- ✅ 全局限流 120/min + 认证接口限流 10/min + 验证码限流 5/小时（防暴力破解）
- ✅ 升级 multer→2.x、express→4.22、socket.io→4.8（清零高危依赖漏洞）

### 隐私（个人信息保护法合规）
- ✅ 企业详情接口 `select` 白名单，不再泄漏营业执照/身份证/手机号
- ✅ 供应公司公开接口去除营业执照、联系人、手机号
- ✅ 分享信息流去除作者手机号
- ✅ 投递列表去除身份证号、手机号、邮箱等敏感字段
- ✅ seed 脚本移除明文凭证日志

### 授权
- ✅ Socket.IO 强制 token 鉴权（移除可冒充任意用户的旧版兼容分支）
- ✅ 注册接口禁止自选 ADMIN 角色

### 维护
- ✅ 健康检查增加数据库探活
- ✅ 优雅关闭（SIGTERM/SIGINT）+ 异常兜底捕获
- ✅ 配置 `.env.example` 文档

---

## 六、运维操作

### 服务管理（PM2）
```bash
pm2 status                          # 查看进程状态
pm2 logs canlie-backend             # 查看后端运行日志（不含验证码或凭据）
pm2 restart canlie-backend          # 重启后端
pm2 restart ecosystem.config.cjs --update-env   # 重启并刷新环境变量
```

### 数据库（Prisma + SQLite）
```bash
cd backend
npx prisma studio                   # 可视化查看/编辑数据
npx prisma db push                  # 应用 schema 变更
npm run seed                        # 重新播种演示数据
```

### 前端构建
```bash
cd frontend
npm run build                       # 构建到 dist/，由后端生产模式服务
```

### 重启隧道后获取新公网地址
```bash
tail -20 logs/tunnel-out.log | grep trycloudflare
```

---

## 七、上线前待办（建议）

以下为本期未做、上线生产前建议完成的事项：

1. **配置真实短信服务**（阿里云/腾讯云短信或合规供应商）——未配置时重置接口统一返回服务不可用；验证码不得出现在 API、页面或日志中，并应轮换全部测试账号密码。
2. **敏感证件隔离存储** —— 将营业执照/身份证移至 `uploads/private/`，由鉴权接口提供访问（当前 `/uploads` 公开，证件 URL 泄漏源已切断，但可进一步纵深防御）。
3. **数据库定期备份** —— SQLite 单文件，建议每日 cron 备份 `dev.db` 到异地。
4. **日志轮转** —— PM2 日志固定文件，建议配置 `pm2-logrotate` 防磁盘写满。
5. **HTTPS 域名** —— Cloudflare Tunnel 免费版 URL 不固定，上线建议绑定自有域名 + Cloudflare。

---

## 八、目录结构

```
餐饮猎头平台/
├── frontend/src/          # React 前端
│   ├── pages/            # 页面（含 supply/、share/ 子模块）
│   ├── stores/           # Zustand 状态（authStore、chatStore）
│   └── api/              # API 封装
├── backend/src/           # Express 后端
│   ├── routes/           # 路由（auth/jobs/talents/enterprises/matches/
│   │                     #        chat/supply/shares/admin/upload/...）
│   ├── middleware/       # 鉴权中间件
│   └── socket.ts         # Socket.IO 实时聊天
├── backend/prisma/        # Prisma schema + SQLite (dev.db)
├── uploads/               # 上传文件
├── logs/                  # PM2 日志
└── ecosystem.config.cjs   # PM2 进程配置
```

---

*交付完成。如有问题，附本说明 + 健康检查结果 `{"status":"ok","db":"ok"}` 即可定位服务正常。*

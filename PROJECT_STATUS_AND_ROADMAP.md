# 餐猎平台总体问题、上线风险与改进路线图

> 审计日期：2026-08-27
>
> 审计范围：`backend`、`frontend`、`miniprogram`、Prisma 数据模型、部署配置、公开交付文档与 Git 跟踪状态
>
> 基线提交：`17cb027`（新增本文档前的 `main`）
>
> 目标：明确“3 人体验测试”“公开测试”“正式上线”各阶段的阻断项、整改顺序与验收标准

## 0. 重要说明

- 本文档可以公开存放，因此**不会重复任何 JWT 密钥、服务器密码或测试账号密码**。
- 本文档是一次工程、产品和上线风险审计，不构成法律意见。涉及人力资源服务许可、网络招聘、个人信息、自动化决策、招聘公平等事项，应由运营主体结合所在地要求向专业律师及主管机关核验。
- 当前结论不是“项目不能做”，而是：功能骨架已经具备，但安全、权限、隐私、构建和部署底座还没有达到邀请外部测试者使用真实数据的标准。

## 1. 一页结论

### 1.1 当前状态

项目已经具备三类角色和主要业务页面：管理员、企业、人才；包含职位、人才资料、投递、匹配、聊天、供应链、分享社区等模块；微信小程序共有 28 个页面。

但当前版本**不建议直接作为公开测试版上线，也不建议让三位测试者录入真实身份证、手机号、履历、婚育、企业证照或聊天隐私数据**。至少先完成本文第 3 节的“3 人体验测试前门槛”。

### 1.2 最关键的八个问题

| 顺序 | 问题 | 当前影响 | 阶段门槛 |
| --- | --- | --- | --- |
| 1 | 公开 Git 历史含 JWT 签名密钥，公开文档/种子数据含测试凭据 | 可能伪造登录身份；公网测试账号可被直接尝试登录 | 任何外部测试前 |
| 2 | 后端 TypeScript 构建失败 | 无法形成可信、可重复的发布包 | 任何外部测试前 |
| 3 | 匹配执行接口缺少角色和职位归属校验 | 任意已登录账号可能对任意职位触发批量计算与写入 | 任何外部测试前 |
| 4 | 网页端和小程序将“记住密码”明文写入本地存储 | 设备、调试工具或恶意脚本可读取密码 | 任何外部测试前 |
| 5 | 人才详情接口公开且隐私模式执行不完整 | 真实姓名、生日、公司、完整履历等信息可能被未登录访问和批量抓取 | 任何真实人才数据前 |
| 6 | 小程序 API 使用明文 HTTP 裸 IP，开发工具关闭域名校验 | 体验版真机可能不可用，且凭据和个人信息传输不安全 | 体验版前 |
| 7 | SQLite 云部署没有明确持久卷、正式迁移和恢复方案 | 服务重建后可能丢失职位、聊天、投递和用户数据 | 任何真实业务数据前 |
| 8 | 没有自动化测试、CI、安全扫描和统一发布门禁 | 每次修改都可能把已修问题重新带回线上 | 公开测试前 |

### 1.3 推荐路线

先把产品收窄为一个可验证的核心闭环：

`注册/登录 → 完善资料 → 企业发布职位 → 人才浏览/投递 → 企业处理 → 双方沟通`

匹配功能先定位为“规则匹配建议”，不作为自动录用或自动淘汰依据；供应链和分享社区保留为后续模块，避免同时承担招聘平台、电商撮合、内容社区三套运营与合规成本。

## 2. 本轮验证结果

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| Git 工作区 | 检查开始时干净 | `main` 与本地 `origin/main` 跟踪引用一致；推送前仍需联网同步远端 |
| 后端 `npm run build` | **失败** | 出现约 15 个 TypeScript 诊断，涉及 Helmet CSP、限流 key 生成、路由参数类型和 Prisma relation 类型 |
| 小程序 `npm run typecheck` | **通过** | 只能说明 TypeScript 类型检查通过，不等于微信开发者工具真机流程通过 |
| 网页端生产构建 | **未取得可信通过结果** | 本轮执行环境受到父目录读取限制；必须在本地/CI 再验证 |
| 小程序生产构建 | **未取得可信通过结果** | 同样受执行环境目录限制；必须在本地/CI 和微信开发者工具再验证 |
| 自动化测试 | **不存在** | 根、后端、网页端、小程序均无 `test` 脚本，也未发现常用测试框架 |
| GitHub Actions | **不存在** | 没有 `.github/workflows`，推送和合并时不会自动构建或扫描 |

结论：不能把当前仓库状态描述为“已全部验证通过”。下一次发布必须保存构建日志、测试结果和体验版验收记录。

## 3. 三人体验测试前的强制门槛（P0）

### P0-1：轮换公开过的密钥和测试凭据

**证据**

- `ecosystem.config.cjs` 当前跟踪一个非占位 JWT 签名密钥，并且从历史提交起已公开。
- `render.yaml` 也包含不适合生产使用的公开 JWT 字面值。
- `交付说明.md`、`交付文档.md`、`CLAUDE.md` 和 `backend/src/seed.ts` 中存在公开测试凭据。

**立即处理**

1. 在服务器/托管平台生成全新的高强度 JWT 密钥，只放入密钥管理或环境变量，旧值永久停用。
2. 使全部现有登录令牌失效，要求重新登录。
3. 配置文件改为生产环境缺少 `JWT_SECRET` 就拒绝启动；仓库只保留变量名和安全示例。
4. 轮换所有能访问公网环境的测试密码；测试账号不得拥有真实用户数据或生产管理权限。
5. 使用 `git filter-repo` 或等价方式清理 Git 历史，再强制更新远端；执行前先做离线备份并通知所有协作者重新克隆。
6. 在 CI 增加 Gitleaks/TruffleHog 等密钥扫描，阻止再次提交。

**验收标准**

- 使用旧密钥生成的令牌全部返回 401。
- 生产进程未设置 `JWT_SECRET` 时启动失败。
- 当前分支和完整 Git 历史均通过敏感信息扫描。
- 公开文档不再包含可登录账号的实际密码。

### P0-2：修复后端构建并建立可重复发布包

**证据**

当前后端 `tsc` 无法通过，主要问题位于：

- `backend/src/index.ts`：Helmet CSP 配置类型不匹配。
- `backend/src/routes/auth.ts`：限流键生成器的调用签名/类型不匹配。
- `backend/src/routes/matches.ts`、`notifications.ts`、`talents.ts`：Express 路由参数被推断为 `string | string[]`，以及 Prisma 查询结果的 relation 类型和代码使用不一致。

**整改**

- 修到 `backend/npm run build` 零错误。
- 在干净环境从锁文件安装依赖并构建，不能依赖开发机残留的 `dist`。
- 将后端、网页端、小程序的检查纳入同一个发布命令和 CI。

**验收标准**

- 全新克隆后执行标准发布命令可以一次成功。
- 构建产物来自本次提交，CI 页面保留通过记录。
- 服务器部署后 `/api/health` 正常，关键接口冒烟测试通过。

### P0-3：补齐匹配接口的角色和资源归属授权

**证据**

`backend/src/routes/matches.ts` 的 `POST /run/:jobId` 只验证“已经登录”，未在计算前要求管理员身份，也未确认当前企业拥有该职位；接口会读取全部活跃人才并写入匹配结果。

**整改**

- 仅允许：职位所属企业的授权成员，或管理员，触发该职位的匹配。
- 对重复触发增加幂等控制、频率限制、审计日志和任务状态。
- 所有对象级操作统一检查“角色 + 资源所有权 + 当前资源状态”，不能只检查是否有 token。

**验收标准**

- 人才账号、其他企业账号访问该接口均得到 403，且数据库零写入。
- 职位所属企业和管理员按规则成功执行，并留下操作人、职位、时间、结果数量审计记录。
- 增加自动化权限矩阵测试，覆盖管理员、所属企业、其他企业、人才、未登录五类身份。

### P0-4：停止明文保存密码，修复密码找回闭环

**证据**

- `frontend/src/pages/Login.tsx` 将 `remembered_password` 写入 `localStorage`。
- `miniprogram/src/pages/login/index.tsx` 将同一字段写入微信本地存储。
- 旧版本曾在客户端校验固定验证码并把验证码写入日志；当前修复要求由服务端生成一次性随机码，仅保存带密钥摘要，短时有效且验证成功立即消费。

**整改**

- “记住我”最多保存账号标识，不保存密码；已有版本升级时主动删除旧键。
- 网页端和小程序都调用同一套服务端验证码校验，不得在客户端判断固定验证码。
- 接入真实短信或已核验的认证方式；未接入前关闭“忘记密码”入口，改为人工受控重置。
- 验证码日志不得记录完整手机号和验证码；加入有效期、一次性、次数限制和风控。

**验收标准**

- 浏览器和微信存储中搜索不到任何明文密码。
- 任意固定验证码均不能绕过服务端校验。
- 验证码请求、校验、过期、重复使用、频率限制均有自动化测试。

### P0-5：收紧人才详情与敏感字段访问

**证据**

`backend/src/routes/talents.ts` 的 `GET /:id` 当前不要求登录，返回 `userId`、姓名、性别、出生年月、籍贯、当前公司、薪资范围和完整工作经历。`privacyMode` 目前只在值为 `ANONYMOUS` 时改写姓名，没有完整执行私密/隐身、联系方式可见范围和字段级授权。

数据模型还包含身份证号、手机号、邮箱、婚姻、子女、父母信息、企业证照和身份证图片等高风险字段。匹配结果也可能向企业返回婚育、家庭等信息。

**整改**

- 人才列表和详情默认只返回去标识化、最小化的职业信息。
- 真实身份、联系方式和完整履历必须同时满足：人才明确授权、企业已审核、存在合理业务关系、访问被审计。
- 真正实现 `REAL_NAME`、`ANONYMOUS`、`PRIVATE/STEALTH` 和 `contactPrivacy` 的服务端策略；前端隐藏不是安全控制。
- 移除 API 中不必要的 `userId`、出生月、籍贯、当前公司、家庭信息等字段。
- 对详情查询增加限流、防枚举、批量抓取监测和访问日志。

**验收标准**

- 未登录请求只能得到允许公开的最小字段，或直接返回 401。
- 各隐私模式逐字段快照测试通过。
- 未授权企业无法通过修改 URL/ID 获取隐藏数据。
- 每次查看敏感资料都有可追溯访问记录。

### P0-6：将小程序网络链路改为 HTTPS 合法域名

**证据**

- `miniprogram/src/constants.ts` 将 API 基址硬编码为 `http://` 裸 IP。
- `miniprogram/project.config.json` 设置了 `urlCheck: false`。这只方便开发者工具调试，不能代替体验版/正式版的微信合法域名要求。

**整改**

- 准备已备案、证书有效的 HTTPS API 域名，禁止凭据通过 HTTP 传输。
- 在微信公众平台配置 `request`、`uploadFile`、`downloadFile`、`socket` 等实际使用的合法域名。
- 开发、测试、生产地址环境化；构建时选择环境，不再手改源码。
- 真机确认登录、上传、图片访问、聊天 WebSocket、分享回跳等链路。

**验收标准**

- 打开微信开发者工具的域名校验后仍能工作。
- 体验版在三台真实手机上完成核心闭环，网络面板无 HTTP 和非法域名请求。
- API 证书链、过期监控和自动续期已配置。

### P0-7：保证测试数据不会因重启或重部署丢失

**证据**

- Prisma 当前使用 SQLite。
- Render/Railway 配置未体现可靠持久卷或外部数据库资源。
- 仓库没有 `backend/prisma/migrations`，部署配置使用 `prisma db push`，而根脚本又声明 `prisma migrate deploy`，流程不一致。

**整改**

- 三人测试前：确认 SQLite 文件位于持久化目录，建立每日自动备份和一次真实恢复演练。
- 公开测试前：迁移 PostgreSQL，建立可审查、可回滚的 Prisma migration。
- 将“迁移、备份、恢复、回滚”写成唯一部署手册，禁止发布时临时 `db push` 修改生产结构。

**验收标准**

- 重启和重新部署后测试数据仍存在。
- 能在隔离环境从备份恢复到规定时间点，并记录恢复耗时。
- 数据库变更由版本化 migration 完成，CI 可在空库重放全部 migration。

### P0-8：清理 Git 中的上传文件并建立媒体治理

**证据**

`backend/uploads` 目前有 5 个图片文件已经被 Git 跟踪，约 9.4 MB；`.gitignore` 不能自动移除历史中已经跟踪的文件。上传接口仅按文件扩展名允许图片、文档和视频，未验证真实文件类型，也没有对象归属、病毒扫描、保留期和删除流程。

**整改**

- 先人工确认这 5 个文件是否含个人或企业信息；按潜在隐私数据处理。
- 从当前仓库和必要的 Git 历史移除；以后上传到私有对象存储，不进入代码仓库。
- 校验文件头/MIME、限制尺寸和分辨率、随机化对象键、鉴权下载、短期签名 URL、恶意文件扫描。
- 建立用户删除、账号注销、保留期限到期后的媒体清理任务。

当前 `backend/src/index.ts` 还把整个上传目录作为无需业务授权的静态资源发布。随机文件名不是访问控制；身份证、营业执照、工资流水、证明材料等不能使用永久公开 URL。

**验收标准**

- `git ls-files backend/uploads` 无结果，历史扫描满足既定要求。
- 伪装扩展名、超限文件、未授权读取均被拒绝。
- 删除业务对象后，其媒体按照保留策略同步清理并有审计记录。

### P0-9：让禁用、改密和改角色立即撤销旧会话

**证据**

`backend/src/middleware/auth.ts` 签发 7 天 JWT，并在请求时只验证签名，随后直接信任 token 中的 `userId` 和 `role`；没有重新核对用户当前状态、角色或会话版本。管理员禁用账号后，旧 token 可能继续工作直到过期。

**整改**

- 引入 `tokenVersion`、可撤销会话表或等价机制。
- 禁用账号、修改密码、修改角色和密钥轮换时，立即撤销所有旧会话。
- 管理员使用短会话和 MFA；禁止禁用自己或最后一个可用超级管理员。
- 管理接口不能返回密码哈希等内部认证字段；`backend/src/routes/admin.ts` 当前直接返回更新后的完整 User，需要改为字段白名单。

**验收标准**

- 账号被禁用或修改密码后，原 token 的下一次请求立即返回 401/403。
- 角色降级后，旧 token 不能继续执行原角色操作。
- 管理员响应和日志中永不出现密码哈希。

### P0-10：修复认证材料的批量赋值越权

**证据**

`backend/src/routes/talents.ts` 的认证材料接口使用 `const { type, ...data } = req.body`，再把 `...data` 直接交给 Prisma。客户端可能注入 `status`、`talentId` 或其他本不应由客户端控制的字段。

**整改**

- 对每种认证材料使用 Zod 白名单，只接收明确允许的字段。
- `talentId`、`status`、`id`、创建/更新时间全部由服务端生成，新材料固定为 `PENDING`。
- 认证类型使用共享枚举，管理员审核与人才提交接口完全分开。

**验收标准**

- 人才提交 `status: VERIFIED`、其他人的 `talentId`、未知类型或多余字段时返回 400/403，数据库零变化。
- 审核状态只能由有权限的管理员通过专用接口修改，并写入审计日志。

### P0-11：修复企业跨职位修改投递状态

**证据**

`backend/src/routes/jobs.ts` 的 `PATCH /:id/applications/:appId` 先确认 URL 中的职位属于当前企业，却只按 `appId` 查询和更新投递，没有确认该投递的 `jobId` 就是 URL 中的职位；`status` 也可以直接写入任意字符串。

**整改**

- 使用投递 ID 与职位 ID 的联合条件查询，再核验职位归属。
- 用服务端状态机限制合法状态与合法跳转，拒绝任意字符串。
- 状态变化记录操作者、旧值、新值、原因和时间。

**验收标准**

- 企业 A 将自己的职位 ID 与企业 B 的投递 ID 组合时得到 403/404，且数据不变。
- 非法状态及非法跳转得到 400；合法跳转产生审计记录和一次通知。

### P0-12：限制任意账号私聊任意用户

**证据**

`backend/src/routes/chat.ts` 的发送接口只确认收件人存在，没有核验申请、邀请、匹配、联系人隐私或拉黑关系；Socket 路径也需要同样的服务端授权。公开人才接口返回的内部 `userId` 进一步放大了骚扰风险。

**整改**

- 只有存在合法业务关系且未被拉黑的双方才能创建/继续会话。
- HTTP 与 Socket 共用同一授权策略；Socket 握手时验证 token，不只依赖客户端加入房间。
- 增加频控、拉黑、举报、反骚扰和消息幂等 ID。

**验收标准**

- 随机用户 ID、私密人才、已拉黑关系和不相关角色之间均不能发消息。
- 伪造 Socket 房间或收件人 ID 无法绕过权限。

### P0-13：测试版关闭“伪付费”入口

**证据**

`backend/src/routes/enterprises.ts` 的订阅购买接口收到 `planId` 后直接创建 `ACTIVE` 订阅，没有真实支付订单、支付回调、验签、幂等、退款或对账；当前逻辑还允许无订阅继续发布职位。

**整改**

- 三人测试版隐藏价格、购买与付费权益，明确标注“测试期免费”，由服务端测试白名单控制功能。
- 不创建会被误解为真实支付成功的订阅记录。
- 商业化时再实现微信支付服务端订单、回调验签、幂等、金额整数分、退款/对账，以及订阅和职位配额的数据库事务。

**验收标准**

- 未收到可信且验签成功的支付回调时，不能出现 `ACTIVE` 付费权益。
- 体验版没有可能让测试者误认为发生真实交易的入口或文案。

## 4. 公开测试前必须完成（P1）

### P1-1：把“AI 匹配”改造成可解释、可复核的辅助建议

当前实现本质上是确定性的加权规则，不是经过训练和验证的 AI 模型。规则使用性别、年龄、婚姻、子女、家庭稳定性等字段参与过滤或评分，存在招聘公平、个人信息最小化和自动化决策风险。

建议：

- 产品文案改为“匹配建议/规则匹配分”，不要承诺“AI 精准决定”。
- 性别、婚育、家庭、籍贯等敏感或高争议字段默认不用于排序、过滤和评分；确需处理时先完成法律依据、必要性和影响评估。
- 给人才和企业展示分数构成、数据来源、更新时间和可能误差。
- 结果只辅助人工判断，不自动拒绝/录用；提供更正、申诉、人工复核和退出个性化匹配的渠道。
- 为每次算法/权重版本建立离线评估、偏差检查和版本记录。

### P1-2：补齐隐私、协议、用户权利与运营资质

当前登录页出现“同意《服务协议》和《隐私政策》”，但未发现可点击、可保存、带版本号的正式协议页面，也未形成撤回同意、账号注销、查阅/更正/删除、投诉和联系运营方的完整流程。

公开测试前至少完成：

- 运营主体名称、联系方式、处理目的、字段范围、保存期限、共享对象、用户权利、未成年人规则。
- 首次收集前分层告知；对身份证件、企业证照、精准联系方式等敏感信息单独说明必要性并取得适当授权。
- 账号注销和数据删除入口；不能只依靠人工聊天。
- 个人信息保护影响评估：敏感信息、自动化决策、公开展示和第三方处理。
- 核验人力资源服务许可、网络招聘相关要求、ICP/公安备案、微信小程序类目与主体资质。是否需要具体许可应由运营主体向所在地主管机关确认。

### P1-3：建立内容和聊天安全能力

分享社区、评论、私聊和视频上传会带来违法内容、骚扰、虚假招聘、诈骗和版权风险。目前需要补齐：

- 举报、拉黑、投诉、删除、申诉和后台处置闭环。
- 敏感词/图片/视频审核，人工复核队列和处置时限。
- 聊天频控、防陌生人骚扰、虚假招聘提示和证据留存规则。
- 管理员操作审计；删除/封禁必须记录操作者、原因、时间和申诉结果。
- 用户生成内容的服务协议、版权声明和保留期限。

### P1-4：建立测试和 CI 质量门禁

最低 GitHub Actions 流水线：

1. 锁文件安装依赖。
2. 后端 Prisma Client 生成与 TypeScript 构建。
3. 网页端 TypeScript + Vite 构建。
4. 小程序 `typecheck` + `build:weapp`。
5. 单元/接口/端到端测试。
6. 依赖漏洞、密钥、恶意文件和许可证扫描。
7. 只有全部通过才允许部署。

首批自动化测试应覆盖：

- 注册、登录、令牌过期、修改密码、找回密码。
- 管理员/企业/人才/未登录的接口权限矩阵。
- 企业审核、职位归属、职位发布/关闭。
- 人才隐私模式、联系方式授权和资料访问审计。
- 投递状态机、重复投递、撤回和越权修改。
- 匹配幂等、权限、规则版本和敏感字段排除。
- 聊天会话成员校验、举报/拉黑。
- 上传类型、大小、恶意文件、归属和删除。

### P1-5：确定唯一部署事实来源

仓库同时存在 Render、Railway、Nixpacks、PM2、Nginx、Cloudflare Tunnel 等方案，README 与部署文档的推荐路径不一致。建议确定：

- **测试环境**：唯一域名、唯一部署平台、独立数据库、测试数据、日志和密钥。
- **生产环境**：独立资源和账号，不复用测试密钥、测试用户或数据库。
- `DEPLOY.md` 作为唯一运行手册，其他文档只链接到它。
- 部署包含健康检查、数据库迁移、失败回滚、备份验证和发布记录。

### P1-6：补齐可观测性与故障处置

- 结构化日志使用请求 ID，默认脱敏手机号、token、验证码、身份证号和聊天正文。
- 接入错误追踪、接口延迟、5xx、数据库容量、磁盘容量、WebSocket 连接和证书到期告警。
- `uncaughtException` 后不应继续运行在未知状态；记录后优雅退出，由进程管理器拉起。
- 制定安全事件和数据泄露应急预案，明确联系人、隔离、轮换、通知和复盘步骤。

### P1-7：改善小程序包体、导航与真机体验

28 个页面目前全部进入主包，未使用分包；底部导航通过 `Taro.reLaunch` 切页，会丢失滚动位置和筛选状态。建议：

- 主包仅保留登录、职位、通知、个人中心等首屏关键页；管理端、供应链、分享社区拆成分包。
- 使用原生 `tabBar` 或明确的页面栈策略，保留列表状态和返回路径。
- 建立弱网、低端 Android、iPhone 刘海/动态岛、字体放大和键盘遮挡真机矩阵。
- 图片压缩、列表分页/虚拟化、骨架屏和缓存策略都要有性能指标。

### P1-8：建立企业与职位审核状态机

当前企业只要不是 `REJECTED` 就可能发布职位，`PENDING` 企业也未被明确禁止；新职位默认可以处于可见状态，而产品文案又声称平台会审核，前后不一致。

建议：

- 只有 `APPROVED` 企业可提交职位。
- 新职位先进入 `PENDING_REVIEW`，管理员通过后才公开。
- 企业资料关键字段变更后重新审核；企业被拒绝/禁用时暂停其全部职位。
- 增加职位下架、违规原因、审核人、审核时间和申诉流程。

### P1-9：统一接口输入校验、状态和前后端契约

项目已安装 Zod，但后端没有形成统一请求 schema。多数分页没有上限，多种状态以任意字符串写入；网页端、小程序、Prisma 注释之间已经出现隐私枚举和投递状态漂移，前端提交的部分字段也可能未被后端保存。

建议：

- 所有创建/更新接口使用 Zod 白名单，拒绝未知字段。
- 状态改为 Prisma enum 或共享 TypeScript 枚举，并实现显式状态机。
- 分页统一限制，例如 `1 <= pageSize <= 100`。
- 用 OpenAPI/共享 schema 生成两端 DTO 与客户端，后端契约作为唯一事实来源。
- 对薪资、年龄、年月、手机号、身份证、URL 等值做完整范围和格式校验；更新时对“合并后的最终对象”验证整体约束。

### P1-10：提高聊天的实时性、一致性和可恢复性

除了 P0 的联系权限外，聊天还存在会话唯一性、不同职位会话混合、多步写入无事务、Socket 握手认证不足和小程序轮询整段消息等问题。

建议：

- 采用明确 `conversationId`，参与者建立唯一约束，职位上下文作为会话属性。
- 消息创建、会话摘要、未读数和通知在事务中更新。
- 使用游标增量同步、客户端幂等 ID、送达/已读状态和断线重连。
- 用集成测试验证网页和小程序 Socket URL、namespace/path、鉴权、重连和跨端收发。

### P1-11：修复数据模型、索引、事务与金额表达

当前多个枚举和 JSON 以字符串保存，菜系/业态用逗号分隔；部分通知、聊天、订阅关系缺少数据库外键或唯一约束；金额使用浮点数，多步业务操作普遍缺少事务。

建议：

- 高频筛选和排序字段增加复合索引，并用真实查询计划验证。
- 核心关系增加外键、唯一约束和删除策略，避免孤儿数据。
- 结构化数据使用关系表或经过 schema 校验的 JSON。
- 支付金额统一使用整数分或数据库 Decimal。
- 注册、投递、聊天、评论计数、订阅和配额扣减使用事务与幂等键。

### P1-12：保护管理员账号并建立不可抵赖审计

管理员当前使用普通账号登录；高权限操作缺少统一审计，敏感资料查看也没有查看原因和最小权限分层。

建议：

- 管理员启用 MFA、短会话、设备/IP 风控和紧急账号管理。
- 拆分超级管理员、企业审核、人才审核、内容审核、客服等最小权限角色。
- 用户禁用、企业审核、人才认证、星级、内容处置、敏感资料查看和数据导出全部写入审计日志。
- 审计日志记录操作者、对象、前后值、原因、请求 ID 和时间，并限制普通管理员修改/删除。

### P1-13：治理公开文档与仓库许可证

README、DEPLOY、交付说明、交付文档和 CLAUDE 中存在重复或过时内容，包括固定验证码、部署方案、安全状态和“AI”能力描述。公开仓库目前也没有明确 LICENSE；公开可见不等于允许他人复制和分发。

建议：

- `README.md` 只保留产品简介和本地启动。
- `ARCHITECTURE.md` 记录架构和数据流，`SECURITY.md` 记录漏洞报告与安全基线，`DEPLOYMENT.md` 作为唯一部署手册，`TESTING.md` 记录验收方法而不记录密码。
- 每份文档标注更新时间和适用环境，删除临时隧道、旧 URL、固定验证码和过期截图。
- 根据商业策略选择合适的开源许可证；若不打算授权他人使用，应考虑把仓库设为 Private，并仍然清理历史敏感内容。

## 5. 后续优化（P2）

### P2-1：收窄产品范围

推荐优先级：

1. 招聘核心闭环。
2. 可解释的匹配与企业人才库。
3. 聊天和通知可靠性。
4. 管理审核、运营和数据看板。
5. 供应链与分享社区在核心留存得到验证后再决定是否上线。

每增加一类业务，不只是多几个页面，还会增加权限、审核、客服、内容安全、隐私、统计和测试成本。

### P2-2：统一 Web 与小程序的领域逻辑

网页端和小程序存在大量相似页面与类型定义，容易出现一端修复、另一端遗漏。建议逐步共享：

- API client、错误码、领域类型、校验规则和权限能力。
- 设计 token、状态文案、埋点事件和测试数据工厂。
- 不强求两端视觉 1:1；小程序应优先遵循微信交互、单手操作和移动端信息密度。

### P2-3：设计系统与数据反馈

前端视觉方案在独立设计任务中推进。本项目层面需要先定义：

- 颜色、字号、间距、圆角、阴影、图标、表单、列表、空状态和错误状态 token。
- 每个角色的首页唯一主任务，以及职位卡/人才卡的信息优先级。
- 核心埋点：注册完成率、资料完成率、职位发布率、投递转化、企业处理时长、有效会话率。
- 设计改版必须用可用性测试和转化数据验证，不以“看起来更高级”作为唯一验收标准。

## 6. 建议实施计划

| 阶段 | 建议周期 | 主要工作 | 可以进入下一阶段的条件 |
| --- | --- | --- | --- |
| S0 安全止血 | 0–1 天 | 轮换密钥/密码、使旧 token 失效、暂停真实数据、确认上传文件性质、关闭付费入口 | 旧凭据全部失效，仓库不再继续泄露或产生误导性交易记录 |
| S1 可发布底座 | 1–3 天 | 修复后端构建、HTTPS 域名、环境配置、匹配/认证/投递/聊天对象级授权、明文密码和重置流程 | 干净环境构建通过，权限矩阵和安全冒烟测试通过 |
| S2 三人封闭体验 | 3–5 天 | 独立测试数据库、备份恢复、三类账号、核心闭环真机测试 | 三位测试者只使用虚拟数据，P0 验收全部完成 |
| S3 公开测试准备 | 1–2 周 | PostgreSQL/migration、隐私协议、用户权利、资质核验、内容治理、首批自动化测试和 CI | P1 关键项通过，具备故障与投诉处置能力 |
| S4 产品与体验升级 | 持续 | 设计系统、移动端重构、分包、性能、数据分析、匹配规则评估 | 指标证明改版改善核心转化和留存 |

## 7. 三人测试的最小验收脚本

测试必须使用虚构资料，不上传真实证件，不使用真实企业或人才隐私。

### 管理员

- 登录、退出、令牌过期后重新登录。
- 审核企业资料；拒绝时填写原因。
- 查看关键业务数据，但不能直接看到非必要敏感字段。
- 对举报内容执行隐藏/恢复/封禁，并检查审计日志。

### 企业

- 登录并完善企业测试资料。
- 发布、编辑、关闭一个测试职位。
- 只能查看和操作自己企业的职位、投递和匹配。
- 查看脱敏人才信息，申请授权后查看允许字段。
- 与已建立业务关系的人才聊天。

### 人才

- 注册/登录并完善虚构履历。
- 分别验证实名、匿名、私密/隐身模式。
- 搜索职位、收藏、投递、撤回或查看状态。
- 查看匹配解释、修改资料后重新计算。
- 聊天、举报/拉黑、退出登录、申请注销。

### 共同检查

- iPhone 与 Android 真机、Wi-Fi 与移动网络。
- 首屏、返回、下拉刷新、空状态、错误提示、键盘遮挡。
- 断网、接口超时、token 过期、服务重启。
- 测试结束后删除账号和相关文件，验证备份与日志不泄露敏感信息。

## 8. 发布检查清单

### 3 人体验版

- [ ] JWT 密钥和测试密码已轮换，旧 token 失效。
- [ ] 后端构建通过，网页端和小程序构建有可复现记录。
- [ ] 小程序全部请求使用 HTTPS 合法域名，域名校验开启。
- [ ] 匹配及其他对象级接口的角色/归属权限已修复并测试。
- [ ] 认证材料不能注入审核状态，企业不能跨职位修改投递。
- [ ] 禁用、改密、改角色后旧会话立即失效；管理员响应不含密码哈希。
- [ ] 只有存在合法业务关系且未拉黑的双方可以聊天。
- [ ] 不再明文保存密码，密码找回流程一致且受控。
- [ ] 人才详情和敏感字段访问已最小化。
- [ ] 测试数据库可持久化、已备份并完成恢复演练。
- [ ] 付费入口已关闭并标明测试期免费，不产生虚假 `ACTIVE` 订阅。
- [ ] 只允许三名体验成员，且明确要求使用虚构数据。

### 公开测试版

- [ ] 正式服务协议、隐私政策、单独告知/同意、注销与投诉流程上线。
- [ ] 运营资质、招聘合规、小程序类目、域名备案已核验。
- [ ] 内容举报/拉黑/审核/申诉闭环上线。
- [ ] PostgreSQL 和版本化 migration 上线。
- [ ] CI、核心自动化测试、安全扫描、错误和容量告警上线。
- [ ] 匹配规则完成敏感字段清理、解释、人工复核和影响评估。

### 正式上线

- [ ] 压力、弱网、故障恢复、备份恢复和安全测试通过。
- [ ] 数据保留、删除、访问审计、事件响应按制度运行。
- [ ] 生产和测试完全隔离；无默认密码、测试账户和调试开关。
- [ ] 有明确的值班、客服、投诉、发布、回滚和复盘负责人。

## 9. 暂时不要做的事

- 不要继续在公开文档中列出可登录密码或服务器凭据。
- 不要在密钥轮换前仅删除当前文件中的旧密钥；Git 历史已经公开，删除当前行不能使旧值重新安全。
- 不要用 `urlCheck: false` 作为体验版网络问题的解决方案。
- 不要让测试者录入真实证件、真实履历、真实聊天或企业证照来“顺便试一下”。
- 不要在后端构建失败、没有权限回归测试的情况下继续堆叠新页面。
- 不要把匹配分数包装成自动淘汰或自动录用结论。
- 不要同时对外运营招聘、供应链、社区三条业务线，除非每条线都有相应运营和合规能力。

## 10. 代码证据索引

- 密钥与部署：`ecosystem.config.cjs`、`render.yaml`、`railway.toml`、`nixpacks.toml`、`DEPLOY.md`
- 登录与密码：`frontend/src/pages/Login.tsx`、`miniprogram/src/pages/login/index.tsx`、`frontend/src/pages/ResetPassword.tsx`、`backend/src/routes/auth.ts`
- 鉴权：`backend/src/middleware/auth.ts`、`backend/src/routes/matches.ts`
- 人才隐私：`backend/src/routes/talents.ts`、`backend/prisma/schema.prisma`
- 匹配规则：`backend/src/routes/matches.ts`
- 上传：`backend/src/routes/upload.ts`、`backend/uploads/`
- 小程序网络与页面：`miniprogram/src/constants.ts`、`miniprogram/project.config.json`、`miniprogram/src/app.config.ts`、`miniprogram/src/components/Layout.tsx`
- 构建脚本：根目录及三个子项目的 `package.json`

## 11. 可查的官方参考

以下链接用于确定整改方向；具体适用范围、最新版本和地方执行口径应在正式上线前再次核验：

- [《中华人民共和国个人信息保护法》—中央网信办转载中国人大网](https://www.cac.gov.cn/2021-08/20/c_1631050028355286.htm)
- [《互联网信息服务算法推荐管理规定》—中央网信办](https://www.cac.gov.cn/2022-01/04/c_1642894606364259.htm)
- [《人力资源服务机构管理规定》—中国就业网/人力资源和社会保障部](https://chinajob.mohrss.gov.cn/h5/c/2023-07-03/381610.shtml)
- [《网络招聘服务管理规定》—司法部](https://www.moj.gov.cn/pub/sfbgw/flfggz/flfggzbmgz/202103/t20210309_374224.html)
- [微信小程序网络能力与服务器域名—微信开放文档](https://developers.weixin.qq.com/miniprogram/dev/framework/ability/network.html)
- [微信小程序用户隐私保护—微信开放文档](https://developers.weixin.qq.com/miniprogram/dev/framework/user-privacy/)

## 12. 下一步的默认决策

如果没有新的业务优先级，建议下一轮开发只做一个任务：**完成 S0 + S1，产出可供三人使用虚构数据的封闭体验版**。前端视觉升级可以并行做方案，但不应阻挡安全、权限、构建和 HTTPS 的修复。


# 餐猎平台安全修复、V2 协作与后续开发交接

更新时间：2026-08-27  
仓库：<https://github.com/yangxinyi0731-cmd/canlie-platform>  
微信小程序 AppID：`wx936945c203adb98a`

> 本文件只记录可公开的开发要求、进度和验收结果。不得在本文件或任何 Git 提交中写入手机号、密码、JWT、服务器密码、短信密钥、证件资料或其他真实个人信息。

## 1. 用户的不可变更要求

1. 保护 `main`：从 2026-08-27 起，不得 force push、reset、覆盖、重写历史，不能把 V2 直接合入 `main`。
2. 保留保护要求到达前已经存在于 `main` 的正常提交，不回滚、不丢弃、不改写。
3. 所有后续修复必须放在独立 `bugfix/*` 分支，并尽量拆成范围单一、可审查、可回滚的小提交。
4. 不得把 `ui-v2-mobile`、其他 V2 分支或 V2 工作区直接合并进 `main`；安全修复应在审查后选择性 cherry-pick 到 V2。
5. 修复工作不得覆盖 V2 前端设计，不得用原版页面替换 V2 页面；遇到前端冲突时优先保留 V2 视觉与交互，只移植安全逻辑。
6. 每一批工作结束时必须明确报告：
   - 当前分支名；
   - 最终 commit SHA；
   - 是否已经推送；
   - 是否仍有未提交或未跟踪修改；
   - 是否触碰 `main` 或 V2；
   - 测试、部署与回滚状态。
7. 公开仓库只允许虚构测试资料。任何敏感凭据必须通过仓库外的安全文件或运行环境变量管理。
8. 不得为了“看起来完成”而关闭小程序合法域名校验、绕过 HTTPS、使用裸 IP、临时隧道或伪造备案状态。

## 2. 当前 Git 基线

- 保护要求到达前，安全修复已经正常提交并推送到 `main`。
- 基线提交：`cf3e7c8f9b7dc50eba756e4e629c313b82338712`
- 提交说明：`security: harden authentication and object authorization`
- 当前修复分支：`bugfix/security-authz-https`
- 远端修复分支已经存在并指向上述提交。
- 当前分支创建时工作区干净。
- 没有执行 force push、reset 或历史覆盖。
- 没有合并、覆盖或修改 `ui-v2-mobile`。

提交地址：<https://github.com/yangxinyi0731-cmd/canlie-platform/commit/cf3e7c8f9b7dc50eba756e4e629c313b82338712>

## 3. 第一批四项任务状态

### 3.1 JWT 与公开测试账号轮换——已完成

- 线上 JWT 签名密钥已经更换并重启后端，旧 JWT 已失效。
- 公开文档中出现过的 13 个管理员、企业、人才账号密码已经全部随机轮换。
- 新旧密码登录回归已完成：新密码可用，旧密码不可用。
- 固定万能验证码、前端本地判断验证码、验证码明文响应和日志取码已经删除。
- 密码重置验证码改为一次性随机码，只保存 HMAC 摘要和随机盐；默认 5 分钟过期、最多验证 5 次、成功后立即消费，并带发送/验证限流。
- 尚未接入真实短信服务时，生产接口统一返回 `503`，不能使用万能码或日志取码。
- 当前源码树和提交内容的高置信秘密扫描结果为 0。
- 公开 Git 历史中的旧凭据已经失效，但因 `main` 保护要求，不得通过历史重写和 force push 清除。

本地私密测试账号文件位于仓库外：

`C:\Users\yangxinyi\CANLIE_TEST_CREDENTIALS_20260827.private.json`

该文件不得复制到仓库、聊天记录、Issue、PR、日志或截图中。

### 3.2 后端 TypeScript 编译错误——已完成

- 后端 `npm run build` 通过，TypeScript 编译为 0 错误。
- 修复了 Helmet CSP、Express 5 路由参数类型、`express-rate-limit` v8 IP key，以及匹配相关 Prisma 类型错误。

### 3.3 微信小程序 HTTPS 合法域名——代码保护完成，外部条件未完成

已经完成：

- 删除小程序中的 HTTP 裸 IP API 地址。
- 生产构建必须显式提供 `TARO_APP_API_BASE_URL`。
- 生产构建拒绝缺失配置、HTTP、裸 IP 和 localhost。
- `miniprogram/project.config.json` 保持 `urlCheck: true`。
- 提供未来可用的 Nginx HTTPS 配置模板，但未在证书和备案不存在时强行启用。

当前阻塞：

- `gongchuangweilai.top` 公网返回阿里云 `403 Non-compliance ICP Filing`。
- 该域名目前无法完成 TLS 握手，不能作为微信小程序正式 API 域名。
- 证书挑战配置已安全撤回，服务器上原有 `zhichuangxing` 应用保持正常。

解除阻塞需要以下任一条件：

1. 用户提供一个已经完成网站 ICP 备案、可以修改 DNS 的域名；或
2. 用户完成 `gongchuangweilai.top` 的阿里云网站备案/接入备案。

得到合法域名后的执行顺序：

1. 在独立 bugfix 分支创建小提交，不改 `main`。
2. 配置 DNS 指向服务器。
3. 申请并验证正式 TLS 证书。
4. 启用 `/canlie` 和 `/canlie/socket.io` HTTPS 反向代理，不影响端口 3000 的原应用。
5. 设置小程序生产 `TARO_APP_API_BASE_URL`。
6. 在微信公众平台配置 request、uploadFile、downloadFile 合法域名。
7. 重新构建并用体验版真机验证；不能只依赖开发者工具关闭校验。

### 3.4 匹配、认证材料、投递状态与聊天越权——已完成并部署

已完成：

- 匹配运行、列表和详情只允许职位所属企业或管理员。
- 非管理员查看匹配详情必须存在同职位有效匹配或投递关系。
- 重跑匹配会清理陈旧结果，陈旧 Match 不再充当聊天授权。
- 人才只能读取自己的匹配职位。
- 认证材料使用严格字段白名单，拒绝客户端设置 `talentId`、`status` 或额外字段。
- 管理员认证审核只接受 `VERIFIED` 或 `REJECTED`。
- 投递状态更新同时校验企业、职位和投递记录，使用受控状态机与并发条件更新。
- 相同投递状态属于无副作用重试，不重复通知。
- HTTP 与 Socket.IO 共用聊天授权；仅允许具有同职位投递或有效匹配关系的企业与人才聊天。
- 禁止自聊、同角色聊天、冒充 Socket 发送者和使用历史会话记录绕过授权。

验证结果：

- 后端安全测试：16/16 通过。
- 网页前端生产构建：通过。
- 小程序类型检查和带 HTTPS 地址的生产构建：通过。
- 线上对象授权回归通过：非法跨角色、跨企业、任意人才详情、批量赋值和同角色聊天请求均返回预期的 `400/403`。
- `canlie-backend`、餐猎网页和服务器原有 `zhichuangxing` 应用健康检查均为 `200`。

## 4. 后续第 5–8 项任务

### 4.1 第 5 项：删除网页端和小程序明文保存密码

- 网页端不得把密码写入 `localStorage`。
- 小程序不得把密码写入微信本地存储。
- “记住我”最多保存手机号或非敏感账号标识。
- 新版本启动时应主动删除历史 `remembered_password` 等旧键。
- 必须验证浏览器、小程序存储和构建产物中不存在用户输入密码。
- 向 V2 移植时保留 V2 登录页面视觉，只移植状态管理和清理逻辑。

### 4.2 第 6 项：收紧人才资料、证件、营业执照和上传文件权限

- 人才列表和详情默认只返回最小化、去标识化的职业信息。
- 身份证号、身份证图片、婚育家庭信息、联系方式、工资流水、认证材料和营业执照不得通过公共接口或静态目录公开。
- 上传接口必须记录文件所有者、用途、类型、大小和访问级别。
- 禁止仅凭可猜测 `/uploads/...` URL 读取文件；下载必须做登录、角色和对象级授权。
- 清查已经被 Git 跟踪的 5 个 `backend/uploads` 文件；在确认内容后决定是否从索引和历史中清除，不能未经检查直接删除。
- 验证不同角色、不同企业和未登录用户均不能访问不属于自己的敏感文件。

### 4.3 第 7 项：三人体验阶段关闭虚假付费入口并只用虚构资料

- 所有未接真实支付的入口必须隐藏、禁用或明确标注为不可用，不得伪装支付成功。
- 禁止体验者录入真实身份证、证件、工资流水、营业执照或个人联系方式。
- 为管理员、企业、人才准备完全虚构且相互独立的测试资料。
- 测试数据应有统一标识，并制定体验结束后的清理方式。
- 在 HTTPS 合法域名和第 5、6 项完成前，不得邀请三个人使用真实资料测试。

### 4.4 第 8 项：数据库持久化、备份与恢复

- 明确测试和生产环境的数据库事实来源，不依赖易丢失的临时 SQLite 文件。
- 数据库、上传文件、运行密钥和日志必须与代码发布解耦。
- 制定自动备份频率、保留周期、加密、异地副本和权限。
- 编写可重复的恢复步骤并实际完成一次恢复演练。
- 部署前后都要记录数据库版本、迁移状态和回滚点。
- 不得在没有备份和恢复验证的情况下执行破坏性迁移。

## 5. 新对话的工作规则

新对话开始后必须按以下顺序执行：

1. 完整阅读本文件和仓库根目录的 `PROJECT_STATUS_AND_ROADMAP.md`。
2. 检查 `git status --short --branch`、当前 SHA、远端分支和 `ui-v2-mobile` 的存在状态。
3. 不切换、覆盖、reset 或修改 `main`；不自动合并 V2。
4. 当前第一批修复基线使用 `bugfix/security-authz-https`。
5. 第 5–8 项应根据实际冲突情况新建一个或多个更小的 `bugfix/*` 分支，不把互不相关的整改塞进同一提交。
6. 遇到 V2 前端冲突时，先报告冲突文件和移植方案；保留 V2 UI，只移植安全与数据逻辑。
7. 每项修复必须先写或补测试，再构建、做秘密扫描、提交、推送和报告。
8. 未经用户明确授权，不重写 Git 历史、不 force push、不删除可能含真实资料的文件、不提交备案/短信/支付等第三方高风险表单。

## 6. 每次交付报告模板

```text
任务：
修复分支：
起始 SHA：
最终 SHA：
是否推送：
是否触碰 main：
是否触碰/合并 V2：
本地未提交修改：
测试结果：
线上部署状态：
回滚位置：
剩余阻塞：
```


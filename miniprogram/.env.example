# Required for production builds. The host must be HTTPS, ICP-filed where
# required, and configured as a legal request/upload/download domain in WeChat.
TARO_APP_API_BASE_URL=https://api.your-domain.example/canlie
